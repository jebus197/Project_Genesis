services:
  - type: web
    name: project-genesis
    runtime: python
    buildCommand: pip install -e ".[web]"
    startCommand: uvicorn genesis.web.app:create_app --factory --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
